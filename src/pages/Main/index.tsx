import React, { FC } from 'react';
import { Footer, Header, Game } from '../../components';

export const Main: FC = () => {
    return (
        <>
            <Header />
            <Game />
            <Footer />
        </>
    );
};
