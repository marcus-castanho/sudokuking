import React, { FC, useState } from 'react';
import { Footer, Header } from '../../components';

export const Main: FC = () => {
    return (
        <>
            <Header />
            <Footer />
        </>
    );
};
